version: '3.8'

services:
  spring-ai-backend:
    build:
      context: ./spring-ai-vitor
      dockerfile: Dockerfile
    container_name: spring-ai-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_AI_OPENAI_API_KEY=${SPRING_AI_OPENAI_API_KEY}
    restart: unless-stopped
